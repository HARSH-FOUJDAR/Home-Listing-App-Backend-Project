<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= home ? 'Edit Home' : 'Add Home' %> | Airbnb Style</title>
</head>

<body class="bg-gradient-to-br from-gray-100 to-gray-200 min-h-screen">

    <!-- Header -->
    <header class="fixed top-0 left-0 w-full bg-white/90 backdrop-blur-md border-b shadow-sm z-50">
        <%- include("partial/nav") %>
    </header>

    <!-- Form Container -->
    <main class="flex justify-center pt-28 px-4">
        <div class="bg-white w-full max-w-lg rounded-3xl shadow-2xl p-8 hover:shadow-3xl transition duration-300">

            <h2 class="text-3xl font-bold text-gray-800 text-center mb-2">
                <%= home ? 'Edit Your Home' : 'Add Your Home' %>
            </h2>

            <p class="text-center text-gray-500 mb-8">
                Share details to list your place
            </p>

            <form action="<%= home ? '/edit-home' : '/Add-home' %>" method="POST" class="space-y-5">
                
                <% if(home) { %>
                    <input type="hidden" name="id" value="<%= home.id %>">
                <% } %>

                <!-- House Name -->
                <div>
                    <label class="block text-sm font-semibold text-gray-600 mb-1">
                        House name
                    </label>
                    <input type="text" name="houseName" value="<%= home ? home.houseName : '' %>" placeholder="Cozy Studio Apartment" class="w-full rounded-xl border border-gray-300 px-4 py-3 focus:ring-2 focus:ring-black focus:outline-none hover:border-gray-400 transition" />
                </div>

                <!-- Price -->
                <div>
                    <label class="block text-sm font-semibold text-gray-600 mb-1">
                        Price per night
                    </label>
                    <input type="number" name="price" value="<%= home ? home.price : '' %>" placeholder="2500" class="w-full rounded-xl border border-gray-300 px-4 py-3 focus:ring-2 focus:ring-black focus:outline-none" />
                </div>

                <!-- Location -->
                <div>
                    <label class="block text-sm font-semibold text-gray-600 mb-1">
                        Location
                    </label>
                    <input type="text" name="location" value="<%= home ? home.location : '' %>" placeholder="London, UK" class="w-full rounded-xl border border-gray-300 px-4 py-3 focus:ring-2 focus:ring-black focus:outline-none" />
                </div>

                <!-- Rating -->
                <div>
                    <label class="block text-sm font-semibold text-gray-600 mb-1">
                        Rating
                    </label>
                    <input type="number" step="0.1" name="rating" value="<%= home ? home.rating : '' %>" placeholder="4.8" class="w-full rounded-xl border border-gray-300 px-4 py-3 focus:ring-2 focus:ring-black focus:outline-none" />
                </div>

                <!-- Image URL -->
                <div>
                    <label class="block text-sm font-semibold text-gray-600 mb-1">
                        Image URL
                    </label>
                    <input type="text" name="imageUrl" value="<%= home ? home.imageUrl : '' %>" placeholder="https://image-url.com/home.jpg" class="w-full rounded-xl border border-gray-300 px-4 py-3 focus:ring-2 focus:ring-black focus:outline-none" />
                </div>

                <!-- Description -->
                <div>
                    <label class="block text-sm font-semibold text-gray-600 mb-1">
                        Description
                    </label>
                    <textarea name="description" rows="3" placeholder="Something here..." class="w-full rounded-xl border border-gray-300 px-4 py-3 focus:ring-2 focus:ring-black focus:outline-none"><%= home ? home.description : '' %></textarea>
                </div>

                <!-- Submit Button -->
                <button type="submit" class="w-full bg-black text-white py-3 rounded-xl font-semibold text-lg hover:bg-gray-900 hover:scale-[1.02] active:scale-[0.98] transition duration-200">
                    <%= home ? 'Update Home' : 'Publish Home' %>
                </button>

            </form>
        </div>
    </main>

</body>

</html>
